<%= form_tag(books_path, :method => "get") do %>
  <%= text_thing_tag :search %>
<%= submit_tag "Search", :schoolname => nil ,class: "button is-info" %>
  <% end %>

<% @books.each do |book| %>

<p id="notice"><%= notice %></p>

<h1>語学学校一覧</h1>

<table>
  <thead>
    <tr>
      <th>学校名</th>
      <th>学校説明</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.memo %></td>
        <td><%= link_to 'Show', book %></td>
        <td><%= link_to 'Edit', edit_book_path(book) %></td>
        <td><%= link_to 'Destroy', book, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Book', new_book_path %>
